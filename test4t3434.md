# WAI — Tareas derivadas del Stack Tecnológico v1

**Fuente:** `WAI_Stack_Tecnologico_v1.md` (15-sep-2026), cruzado con `architecture/PLAN_DE_TRABAJO.md`
(16-sep, MVP de ingesta + dashboard), los ADR 0001-0005 del monorepo y lo que ya está desplegado.
**Generado:** 2026-09-18. **Horizonte:** 7 semanas, S1 = 15-sep → S7 = 31-oct; go-live **6-nov**
(⚖️ el ADR 0001 dice 25-oct: confirmar con Wideline en la reunión del 25-sep).
**Convención:** `[x]` hecho y verificado · `[ ]` pendiente · `[~]` en curso · ⚖️ lo decide Wideline ·
`§n` = sección del documento de stack · `[key]` necesita credencial · `[dec]` necesita decisión (ADR 0005).
**Responsables:** Gustavo (dev), Ana (PM), Jorge (IT Wideline), Daniel (monday / Power BI), Angel (CargoWise), Ernesto (decide).

---

## 0. Punto de partida (lo que ya existe, no se repite)

- [x] VPS Ubuntu de DEV operativo: `/desarrollo/wai` con Docker Compose, 6 contenedores healthy, `/health` público, Postgres/Redis solo en loopback, ufw 22/80/443, fail2ban — §2.1, §1 "endurecimiento" (18-sep).
- [x] Backend FastAPI + Pydantic v2 + SQLAlchemy async + Alembic (0001 esquemas `core/conv/rules/audit/mart`; 0002 roles, RLS, grants) — §1.
- [x] Portal Angular 22 compilando y servido por Caddy; login OTP, lista y detalle de embarque, auditoría, integraciones (fake) — §1 frontend (variante Angular aceptada en §3.1).
- [x] Cola `arq` sobre Redis con AOF; procesos `worker` y `scheduler` — §1 colas (variante; ver `[dec]` S1.6).
- [x] Puertos con `fake` para LLM, STT, WhatsApp, Graph, Teams, monday, CargoWise, Blob (ADR 0003) — habilita §3.4 "cambiar dos variables".
- [x] Data mart sintético (5 organizaciones, 40 embarques) y seed — §4.3 caché/idempotencia parcialmente.
- [x] CargoWise: export `.db.age` descargado, descifrado con la llave privada y abierto; diccionario de 8 tablas (`shipments`, `transport_legs`, `containers`, `milestones`, `packing_lines`, `custom_fields`, `organizations`, `entry_headers`, todas por `job_key`) — 16-sep, en `/desarrollo/documents`.
- [x] Credenciales validadas: M365 device code, monday, OneDrive — `VALIDACION_CREDENCIALES.md` (16-sep).
- [x] CI: ruff, `alembic check`, pytest con Postgres, evals, trinquetes de cobertura y arquitectura — §1 "calidad de código" y "pruebas".

---

## S1 · 15-19 sep — Cimientos reales: ingesta CargoWise, decisiones, primer commit

- [x] S1.1 Entorno DEV en el VPS (§2.1, §8 equivalentes) — Gustavo.
- [ ] S1.2 **Primer commit del monorepo** y remoto en GitHub; proteger `main`; `develop` como rama de DEV (§1 CI/CD: `develop` → DEV automático, `main` + tag → PROD con aprobación) — Gustavo. *Hoy todo el trabajo está sin versionar.*
- [ ] S1.3 Mover los originales del cliente fuera de `/desarrollo/documents` en texto plano: llave `age` con permisos 600 en `/root/.wai-keys/` (después Docker secret / Key Vault, §1 secretos); borrar el `.db` descifrado cuando no se use — Gustavo.
- [ ] S1.4 **Contrato del data mart v1** (`docs/architecture/data-mart-v1.md`) sobre el diccionario real: mapeo 8 tablas SQLite → esquema `mart` de Postgres, tipos (todo llega `TEXT`, vacíos `''`: casts centralizados), claves (`job_key`), columnas por nivel L0-L3 y denylist — Gustavo + `data_engineer`. Cierra el riesgo F2 del ADR 0001.
- [ ] S1.5 **Adaptador `CARGOWISE_SOURCE=sqlite_export`** (ADR 0006): watcher de carpeta (local primero; OneDrive vía `rclone` después) → descifrar en tmpfs → *upsert* transaccional por snapshot a `mart` → `shred` del plano → invalidar caché Redis. Tests: llave correcta/incorrecta, carga idempotente, conteos por tabla — Gustavo. Fases 1 del plan del server.
- [ ] S1.6 `[dec]` **Cerrar el ADR 0005** con Ana: frontend (Angular, ya construido), colas (`arq` + outbox en Postgres vs Celery/RabbitMQ), proxy (Caddy vs Traefik), router LLM (puerto propio vs LiteLLM), secretos DEV (`.env` vs SOPS+age), observabilidad DEV (mínima vs Grafana stack). Lo que cambie → tarea nueva; lo que no, se cierra — Gustavo + Ana.
- [ ] S1.7 Dar a Jorge el **Redirect URI** (`https://wai.wideline.biz/api/v1/auth/callback` y el de DEV) y pedir `Files.Read` delegado si el watcher va por Graph en vez de `rclone` (§1 Graph, §1 auth internos) — Gustavo → Jorge `[key]`.
- [ ] S1.8 Copiar la lista de verificación §7 a `docs/architecture/azure-compat-checklist.md` y añadirla a la plantilla de PR — Gustavo.
- [ ] S1.9 Corregir el documento de stack antes de que circule: Teams = **Microsoft 365 Agents SDK** (Bot Framework SDK retirado dic-2025); CargoWise = export SQLite cifrado, no API; Graph solo delegado (onboarding por usuario); definir **Wisor**; adjuntar la "Cláusula Octava" — Gustavo → Ana.
- [ ] S1.10 Pedir la VM Azure según §8 (Ubuntu 24.04, D8s_v5 o D4s_v5 si Flexible Server, disco 400 GB, IP fija, NSG 22/80/443, DNS `wai.wideline.biz`, Managed Identity + Key Vault, usuario de despliegue con llave) — Ana → Jorge ⚖️. *Comprometida para el 18-sep; la actual se accede por RDP: confirmar SO.*

**Listo S1:** un `.age` nuevo en la carpeta → en minutos `SELECT count(*) FROM mart.shipments` devuelve datos reales; repo en GitHub con CI verde; ADR 0005 cerrado.

---

## S2 · 22-26 sep — API de datos real, identidad corporativa, definiciones contractuales

- [ ] S2.1 API `/api/v1/shipments` (filtros modo/estado/origen/destino/cliente, paginación **keyset**), `/shipments/{job_key}` (legs, contenedores, milestones, organizaciones), `/organizations`, `/stats`; DTOs con fechas ISO-8601 y casts de pesos/volúmenes; caché Redis TTL 2-5 min invalidada en `ingest.completed` (§4.3) — Gustavo. Fase 2 del plan del server.
- [ ] S2.2 Vistas L0-L3 con RLS sobre `mart` real y tests: L0 no ve financieros ni otros tenants; agregación con k-mínimo en L2/L3 — Gustavo + `security_auditor`.
- [ ] S2.3 **Login internos con Entra ID** (§1 auth internos): MSAL en Angular (Authorization Code + PKCE) o sesión BFF `[dec]`; validación de JWT (firma/audiencia/expiración) en la API; `AUTH_MODE=fake` en dev hasta el Redirect URI `[key]` — Gustavo.
- [ ] S2.4 **RBAC editable** en Postgres (rol → módulo → segmento, §1 autorización): tablas, pantalla de administración, matriz permiso × módulo × acción × nivel — Gustavo.
- [ ] S2.5 Auth externos: enlace mágico por correo + validación contra contactos de CargoWise (`organizations`/contactos del mart) + habilitación comercial (§1 auth externos); OTP existente como fallback — Gustavo.
- [ ] S2.6 ⚖️ Reunión 25-sep: **Wisor / INTTRA** (API o canalización por correo + monday, §1), fecha de go-live, lista de recortes Fase 1, histórico de snapshots sí/no — Ana + Ernesto.
- [ ] S2.7 Definir con Angel el **SLA del export** (horario, aviso de cambios de esquema, quién rota la llave; generar par propio y entregar solo la pública) — Gustavo → Angel ⚖️.
- [ ] S2.8 `pgvector` en la imagen de Postgres (`pgvector/pgvector:pg16`) y migración con la extensión, preparando la KB (§3.5) — Gustavo.

**Listo S2:** un usuario interno entra con su cuenta corporativa y ve embarques reales de su nivel; Swagger navegable; decisiones del 25-sep escritas.

---

## S3 · 29 sep-3 oct — Canales: WhatsApp, Teams, voz; dashboard real

- [ ] S3.1 WhatsApp Cloud API `[key]`: webhook con `hub.challenge`, firma `X-Hub-Signature-256`, deduplicación por `message.id` en Redis (§4.3 idempotencia), respuesta 200 inmediata y proceso en cola `interactivo`; plantillas *utility* enviadas a aprobación — Gustavo; WABA + verificación → Jorge ⚖️.
- [ ] S3.2 Voz: descarga de media → STT (`gpt-4o-mini-transcribe` en DEV, Azure en PROD, §1) → mismo pipeline; audio + transcripción a Blob/MinIO por `StorageProvider` — Gustavo.
- [ ] S3.3 Teams con **M365 Agents SDK** `[key]` en proceso `teams-adapter`; Azure Bot registrado por Jorge; aprobación one-tap con Adaptive Cards — Gustavo → Jorge.
- [ ] S3.4 Colas por prioridad `interactivo` / `proactivo` / `batch` (§4.3) en el broker que resulte de S1.6; un reporte pesado nunca retrasa una respuesta — Gustavo.
- [ ] S3.5 Dashboard Angular sobre datos reales: tabla de shipments con filtros y paginación, detalle con timeline de milestones, KPIs (totales, top rutas, por cliente), indicador "último snapshot" (SSE en `ingest.completed`) — Gustavo + `frontend_specialist`. Fase 4 del plan del server.
- [ ] S3.6 Consulta "¿dónde está mi embarque X?" por WhatsApp (texto o audio) → identidad → L0 → respuesta con fila de auditoría — Gustavo. *Criterio de éxito nº 1 del CLAUDE.md.*

**Listo S3:** demo a Wideline: un cliente piloto pregunta por WhatsApp y recibe su embarque; un interno ve el dashboard real.

---

## S4 · 6-10 oct — Correo, clasificación, claims

- [ ] S4.1 Graph `[key]`: `MailGateway` real (búsqueda KQL, hilos, borradores con firma AI, envío tras aprobación); suscripciones `message`/`event` < 7 días con renovación por usuario y `clientState`; `Retry-After` en 429 — Gustavo.
- [ ] S4.2 Búsqueda inteligente de correos por voz/texto (agente acotado: 3-5 candidatos, elección tipada) + borrador → aprobación — Gustavo.
- [ ] S4.3 Clasificación urgente/importante con prefiltros deterministas + tier chico y salida `enum`; resumen diario **bajo demanda o en ventana repartida**; caps por feature/usuario/tenant (§4.3 límites) — Gustavo.
- [ ] S4.4 **Medir el costo real** con 5 usuarios piloto (volumen de correo 30 días vía Graph) y recalcular el presupuesto de tokens para 100 internos; llevar el número a Wideline ⚖️ — Gustavo + Ana. *El 800-1.000 USD/mes se fijó con 30 internos.*
- [ ] S4.5 Claims externos → ticket **monday.com** `[key]` (GraphQL, idempotencia, webhooks de estatus) + confirmación al cliente + alerta al responsable — Gustavo; plan/límite diario → Daniel ⚖️.
- [ ] S4.6 Onboarding por áreas de los 100 internos (consentimiento delegado uno a uno): guion, orden de las 9 áreas, soporte — Ana.

**Listo S4:** un interno pide por Teams el resumen de urgentes y recibe el borrador para aprobar; un claim por WhatsApp crea el ticket en monday.

---

## S5 · 13-17 oct — Cotizaciones, reglas, reportes, KB

- [ ] S5.1 Cotizaciones: transcripción → extracción (schema FRD §15) → una ronda de faltantes → solicitud estructurada a pricing (correo + ticket) — Gustavo.
- [ ] S5.2 Recordatorios y reglas de tiempo/evento (ETA − N días, "cuando responda este hilo", "si la tarea sigue abierta") en `scheduler`/Beat con horarios en Postgres (§1 scheduler) — Gustavo.
- [ ] S5.3 Reportes PDF/Excel deterministas: WeasyPrint + openpyxl + Plotly (§1 reportes); narrativa LLM solo en el texto; entrega por URL prefirmada — Gustavo.
- [ ] S5.4 Estado de cuenta: Power BI `executeQueries` `[key]` o cartera desde el mart (el export **no trae financieros**) — Gustavo; dataset → Daniel ⚖️.
- [ ] S5.5 Base de conocimiento con `text-embedding-3-small` + pgvector bajo RLS (FAQ, plantillas, políticas, manual de imagen) (§1 embeddings) — Gustavo; contenidos → Wideline ⚖️.
- [ ] S5.6 Alertas de comunicación ("avísame cuando responda", palabra clave) sobre suscripciones Graph — Gustavo.

**Listo S5:** los módulos de valor del FRD demostrables en fake y, donde haya key, en vivo.

---

## S6 · 20-24 oct — Observabilidad, seguridad, PROD en Azure

- [ ] S6.1 OpenTelemetry en api/worker/scheduler con exportador por configuración: OTel Collector + Uptime Kuma en DEV; Grafana/Loki/Prometheus solo si S1.6 lo aprueba; Application Insights en PROD (§1 observabilidad) — Gustavo.
- [ ] S6.2 `/health` y `/metrics` en cada servicio; Caddy/Traefik saca de rotación la réplica que falla (§4.3) — Gustavo.
- [ ] S6.3 Backups: `pg_dump` diario + restic a MinIO/Blob y copia externa; **ensayo de restauración documentado** (§1 backups: "en S1 y repetida en S6"; la de S1 no se hizo: hacerla aquí) — Gustavo.
- [ ] S6.4 Seguridad: suite adversarial (prompt injection por correo/mensaje/ticket, exfiltración PII, IDOR, agregación n<k), `security_auditor` sobre auth/CSRF/sesión, SSH solo con llave en el VPS ⚖️ Gustavo — Gustavo.
- [ ] S6.5 Secretos: `SecretProvider` con Key Vault + Managed Identity en PROD; rotación de la llave `age` y del client secret documentadas (§1 secretos) — Gustavo.
- [ ] S6.6 CI/CD completo: build → GHCR → `develop` despliega al VPS por SSH; `main` + tag despliega a Azure con aprobación manual (§1 CI/CD) — Gustavo.
- [ ] S6.7 **Primer despliegue a la VM Azure** `[key]` con `compose.prod.yaml`: Postgres Flexible Server + Blob + Key Vault + Azure OpenAI (§2.2 "Fase 1 recomendada", ADR 010 del stack ⚖️ costo) — Gustavo → Jorge.
- [ ] S6.8 Recorrer la lista §7 completa contra la imagen desplegada; cada fallo es un bug de S6 — Gustavo + `reviewer`.

**Listo S6:** la misma imagen corre en el VPS y en Azure cambiando solo variables; restauración probada; suite adversarial en verde.

---

## S7 · 27-31 oct — Carga, piloto, documentación, release

- [ ] S7.1 Prueba de carga con **Locust: 200 sesiones simultáneas** con respuestas dependientes del LLM (§4.2 etapa A); ajustar réplicas de `api` (`--scale api=3`), concurrencia de workers y pool de PgBouncer — Gustavo + `tester`.
- [ ] S7.2 Piloto interno (5-10 usuarios) + 1-2 clientes de la primera oleada; KPIs FRD §3.2 en dashboard — Ana + Gustavo.
- [ ] S7.3 Contract tests `live` de cada integración encendida; evals con modelos reales sobre el umbral — Gustavo.
- [ ] S7.4 Runbooks (`docs/runbooks/`): despliegue, rollback, restauración, rotación de secretos y de la llave `age`, renovación de suscripciones Graph, resize de VM (§6 estructura) — Gustavo.
- [ ] S7.5 Documentación de usuario por canal y release note `docs/releases/fase1.md`; demo final y aceptación ⚖️ Ernesto — Ana.
- [ ] S7.6 Buffer 3-6 nov: correcciones del piloto; go-live **6-nov** (o 25-oct si Wideline lo mantiene ⚖️).

---

## Transversales (aplican en cada PR, desde S1)

- [ ] T.1 Servicios sin estado: nada en disco local de api/workers; sesiones en Redis, archivos en object storage (§4.3, §7).
- [ ] T.2 Migraciones compatibles hacia atrás: añadir antes de usar, nunca borrar en el mismo despliegue (§4.3).
- [ ] T.3 Toda configuración por variables de entorno; webhooks con URL configurable (§7).
- [ ] T.4 Imágenes `linux/amd64`, sin rutas absolutas ni dependencias del host (§7).
- [ ] T.5 Flujo swarm: plan → código → `tester` + `reviewer` → `swarm done`; cada regla del gate con su defecto plantado.
- [ ] T.6 Costo por feature medido desde el primer flujo con LLM real; alerta al 70 % (§1 router).

---

## Bloqueantes externos (quién, qué, para cuándo)

| # | Qué | Quién | Bloquea | Fecha |
|---|---|---|---|---|
| B1 | Redirect URI registrado en la app "WAI - Microsoft 365 Integration" | Jorge | S2.3 auth real | 22-sep |
| B2 | VM Azure según §8 + Azure OpenAI (el acceso puede tardar días) + Key Vault + Blob | Jorge | S6.7 | 18-sep (comprometido) → escalar |
| B3 | WABA verificado, número y plantillas | Jorge | S3.1 | 26-sep |
| B4 | Azure Bot para Teams | Jorge | S3.3 | 3-oct |
| B5 | Plan de monday.com y límite diario; dataset Power BI consultable | Daniel | S4.5, S5.4 | 26-sep |
| B6 | SLA del export CargoWise, cambios de esquema, rotación de llave (recibir nuestra pública) | Angel | S1.5, S2.7 | 26-sep |
| B7 | Wisor / INTTRA: API o canalización por correo | Ernesto | S5 | 25-sep |
| B8 | Fecha de go-live (25-oct vs 6-nov), recortes de Fase 1, histórico de snapshots | Ernesto + Ana | todo | 25-sep |
| B9 | Contenidos de la KB (FAQ, políticas, manual de imagen) | Wideline | S5.5 | 10-oct |
| B10 | Autorización para SSH solo con llave y para leer/mover `/desarrollo/documents` | Gustavo | S1.3, S6.4 | ya |
